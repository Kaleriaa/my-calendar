import { styled } from 'styled-components'

export const SettWrapper = styled.div`
    width: 100%;
    height: fit-content;
    background-color: var(--grey);
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 20px;
`
export const SettButton = styled.button`
    border: none;
    background-color: transparent;
    cursor: pointer;
    color: red;
    font-size: 18px;
`
